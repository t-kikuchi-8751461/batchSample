SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='TRADITIONAL,ALLOW_INVALID_DATES';

CREATE SCHEMA IF NOT EXISTS `batch_sample_db` DEFAULT CHARACTER SET utf8 ;
USE `batch_sample_db` ;

CREATE TABLE IF NOT EXISTS `batch_sample_db`.`execution_history` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `successful_termination_executed_at` DATETIME NOT NULL COMMENT '正常終了した実行時刻',
  PRIMARY KEY (`id`))
ENGINE = InnoDB
COMMENT = 'バッチ実行履歴';

CREATE TABLE IF NOT EXISTS `batch_sample_db`.`customer` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `name` VARCHAR(255)  NOT NULL COMMENT '氏名',
  `address` VARCHAR(255)  NOT NULL COMMENT '住所',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '作成時刻',
  `updated_at` DATETIME NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新時刻',
  PRIMARY KEY (`id`))
ENGINE = InnoDB
COMMENT = '顧客情報';

CREATE TABLE IF NOT EXISTS `batch_sample_db`.`product` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `name` VARCHAR(255) NOT NULL COMMENT '製品名',
  `description` VARCHAR(255) NOT NULL COMMENT '製品説明',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '作成時刻',
  `updated_at` DATETIME NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新時刻',
  PRIMARY KEY (`id`))
ENGINE = InnoDB
COMMENT = '製品情報';

CREATE TABLE IF NOT EXISTS `batch_sample_db`.`purchase_history` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `customer_id` BIGINT UNSIGNED NOT NULL COMMENT '顧客ID',
  `product_id` BIGINT UNSIGNED NOT NULL COMMENT '製品ID',
  `purchase_time` DATETIME NOT NULL COMMENT '購入時刻',
  `purchase_quantity` INT UNSIGNED NOT NULL COMMENT '購入数量',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '作成時刻',
  `updated_at` DATETIME NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新時刻',
  PRIMARY KEY (`id`),
  CONSTRAINT `fk_purchase_history_customer`
    FOREIGN KEY (`customer_id`)
    REFERENCES `batch_sample_db`.`customer` (`id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_purchase_history_product`
    FOREIGN KEY (`customer_id`)
    REFERENCES `batch_sample_db`.`product` (`id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION
  )
ENGINE = InnoDB
COMMENT = '購入履歴';

SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
